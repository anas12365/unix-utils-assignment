#include <stdio.h>
#include <unistd.h>
#include <stdlib.h>

void pwd_main() {
    char cwd[1024];
    if (getcwd(cwd, sizeof(cwd)) != NULL) {
        printf("%s\n", cwd);
    } else {
        perror("getcwd");
        exit(1);
    }
}
